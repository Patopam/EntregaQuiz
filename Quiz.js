const GradesEstudents = [100, 30, 40, 80, 10];
const Results = computedGrades(GradesEstudents);
console.log(Results);

var numbers = [100, 30, 40, 80, 10];
var doubles = numbers.map(function (x) {
	return;
});
